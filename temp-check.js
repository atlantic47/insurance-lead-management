const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); (async () => { const t = await prisma.tenant.findFirst({ where: { id: '3524cf31-27c7-42ef-a93f-e622924bf613' }, select: { settings: true } }); const ws = t.settings?.credentials?.whatsapp; console.log('Token in settings:', ws?.accessToken ? ws.accessToken.substring(0, 50) + '...' : 'NOT FOUND'); console.log('Token length:', ws?.accessToken?.length || 0); await prisma.\(); })();
